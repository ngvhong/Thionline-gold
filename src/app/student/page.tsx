// THÊM MỚI (Giai đoạn 0 — bảng chọn vai trò): route hoàn toàn mới, tách
// riêng khỏi toàn bộ code GV. Không import gì từ src/app/page.tsx.
import StudentApp from './StudentApp';

export default function StudentPage() {
  return <StudentApp />;
}
